Нет явно определённого конструктора у LinkedList.
Конечно, в Python это и так работает, но в АТД важно явно описывать интерфейс.

Нет комментария у конструктора TwoWayList.
Тут чистая невнимательность.

В моём коде запрос статуса каждой команды возвращает свой тип,
и информация о результате выполнения команды содержится в значениях этого типа,
а не в комментариях (например: RightStatus.NO_RIGHT_NEIGHBOR).
Благодаря этому несколько ошибок, допущенных из-за невнимательности,
было поймано линтером при проверке типов.
