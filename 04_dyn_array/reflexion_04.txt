Наборы команд в задании и ответе оказались разными,
но принципы описания пред- и постусловий те же.

Лично мне версия из ответа нравится тем, что в ней есть команда
изменения значения элемента массива с заданным индексом,
которую можно реализовать со сложностью O(1).
Для подобной операции в версии из задания пришлось бы удалять и вставлять элементы,
дважды сдвигая "хвост" массива, что имеет сложность O(N).

В этот раз в моей версии пред- и постусловия описаны достаточно подробно.
